server {
    listen 443 ssl;
    server_name mado.naide.moe;

    # TLS managed by certbot or your preferred method

    # Flutter web — SPA fallback
    root /home/web/apps/mado_front_artifacts;
    index index.html;

    location ~* ^/(index\.html|flutter_bootstrap\.js)$ {
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        expires 0;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Crawlers on card pages → FastAPI OG endpoint
    location ~ ^/set/([^/]+)/card/(.+)$ {
        if ($http_user_agent ~* "(Slackbot|Twitterbot|facebookexternalhit|LinkedInBot|WhatsApp|Discordbot|TelegramBot)") {
            proxy_pass http://127.0.0.1:8013/og/set/$1/card/$2;
            break;
        }
        try_files $uri /index.html;
    }

    # OG / OpenGraph API
    location /og/ {
        proxy_pass http://127.0.0.1:8013;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # GraphQL API
    location /graphql {
        proxy_pass http://127.0.0.1:8013;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

server {
    listen 80;
    server_name mado.naide.moe;
    return 301 https://$host$request_uri;
}
